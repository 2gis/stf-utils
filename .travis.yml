language: python
python:
  - "2.7"
  - "3.5"
install:
  - pip install -r requirements.txt
  - pip install -r test-requirements.txt
script:
  lode_runner -vs tests/ --with-xunit
after_success:
  - coverage xml
  - codecov